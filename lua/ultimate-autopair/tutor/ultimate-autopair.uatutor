# Ultimate-Autopair's Config Interactive Tutorial

# -Keymaps
{Be able to dynamically change the keymappings}

!HELP `<localleader>h`
When hovering over `:h *`, open the help.

!ACTION `<localleader>a`
When hovering over most codeblocks, check that the config do expected results...

# Dynamic pairs
{lua string multiline pair with `=`}

Read: `:h {dynamic-pair}`, `:h {function}`.

```lua [CONFIG]
{
}
```
```. [EXPECTED RESULTS]
[=|] + [ -> [=[|]=]
```
## HINT 1
To create a dynamic pair, do `{{'[',function(...) end},{']',function(...) end}}`
## HINT 2
Look into `:h {example-html-tag}`
## SULUTION
```. [SULUTION]
...
```

<!-- vim: set filetype=markdown conceallevel=0 sw=2 et: -->
